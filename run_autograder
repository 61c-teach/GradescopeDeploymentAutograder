#!/usr/bin/env bash

source /autograder/source/settings.sh

# Update autograder files
cd $AGROOT
git pull origin $BRANCH
git submodule update --init --recursive

chmod +x run_autograder
./run_autograder
